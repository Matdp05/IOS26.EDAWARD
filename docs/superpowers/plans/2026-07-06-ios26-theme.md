# IOS26.EDAWARD — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thème Spicetify « Liquid Glass » iOS 26 : panneaux de verre flous et flottants, îlot de lecture, accent bleu iOS, fond dynamique tiré de la pochette en cours.

**Architecture:** Trois fichiers à la racine du thème. `color.ini` fournit les palettes dark/light via les variables `--spice-*`. `user.css` rend les panneaux Spotify translucides (`backdrop-filter`) et flottants, et style tous les composants. `theme.js` injecte un calque fixe plein écran (`#ios26-backdrop`) affichant la pochette floutée, mis à jour à chaque `songchange`.

**Tech Stack:** Spicetify 2.44.0, Spotify 1.2.93, CSS (Chromium/CEF), JavaScript vanilla (API Spicetify).

## Global Constraints

- Aucune mention de Claude ou d'IA nulle part : ni commits, ni fichiers, ni README.
- Accent : `0A84FF` (dark) / `007AFF` (light). Erreur : `FF453A` / `FF3B30`.
- Toutes les couleurs du CSS viennent de `--spice-*` / `--spice-rgb-*` (jamais de hex codé en dur, hors color.ini).
- Assets uniquement en data-URI inline (pas de fichiers image).
- Vérification de chaque tâche : `spicetify apply` + contrôle visuel dans Spotify.
- Dossier du thème = répertoire de travail : `C:\Users\HP\AppData\Roaming\spicetify\Themes\IOS26.EDAWARD`.

---

### Task 1: color.ini + activation du thème

**Files:**
- Create: `color.ini`

**Interfaces:**
- Produces: variables `--spice-text`, `--spice-subtext`, `--spice-main`, `--spice-sidebar`, `--spice-player`, `--spice-card`, `--spice-button`, `--spice-rgb-*` etc., consommées par les tâches 2–4.

- [ ] **Step 1: Écrire color.ini**

```ini
[dark]
text               = FFFFFF
subtext            = B8B8C2
main               = 0A0A0F
sidebar            = 15151E
player             = 15151E
card               = 1E1E28
shadow             = 000000
selected-row       = FFFFFF
button             = 0A84FF
button-active      = 0A84FF
button-disabled    = 3A3A45
tab-active         = 2A2A36
notification       = 0A84FF
notification-error = FF453A
misc               = 8E8E93

[light]
text               = 1C1C1E
subtext            = 6E6E73
main               = EDEDF4
sidebar            = FBFBFD
player             = FBFBFD
card               = FFFFFF
shadow             = 9A9AA5
selected-row       = 1C1C1E
button             = 007AFF
button-active      = 007AFF
button-disabled    = C7C7CC
tab-active         = E3E3EA
notification       = 007AFF
notification-error = FF3B30
misc               = AEAEB2
```

- [ ] **Step 2: Activer le thème et appliquer**

Run: `spicetify config current_theme IOS26.EDAWARD; spicetify config color_scheme dark; spicetify apply`
Expected: `success` sur les trois commandes, Spotify redémarre avec la palette sombre (fond quasi noir, accents bleus), disposition encore standard.

- [ ] **Step 3: Commit**

```bash
git add color.ini
git commit -m "feat: palettes dark et light iOS"
```

### Task 2: user.css — matériau verre + disposition flottante

**Files:**
- Create: `user.css`

**Interfaces:**
- Consumes: variables `--spice-*` de la tâche 1.
- Produces: variables CSS `--ios-radius-lg/md/sm`, `--ios-gap`, `--glass-border`, `--glass-highlight`, et l'attente d'un élément `#ios26-backdrop > .layer` (stylé ici, créé en tâche 4).

- [ ] **Step 1: Écrire la base de user.css**

```css
/* IOS26.EDAWARD — Liquid Glass */

:root {
  --ios-radius-lg: 24px;
  --ios-radius-md: 16px;
  --ios-radius-sm: 12px;
  --ios-gap: 8px;
  --glass-blur: 42px;
  --glass-border: rgba(var(--spice-rgb-text), 0.12);
  --glass-highlight: rgba(255, 255, 255, 0.28);
  --glass-bg: rgba(var(--spice-rgb-sidebar), 0.52);
  --glass-bg-strong: rgba(var(--spice-rgb-card), 0.68);
  --ios-shadow: 0 8px 32px rgba(var(--spice-rgb-shadow), 0.35);
}

html, body, #main, .Root, .Root__top-container {
  background: transparent !important;
  font-family: "SF Pro Display", "SF Pro Text", "Segoe UI Variable Display",
    "Segoe UI", system-ui, sans-serif !important;
}

/* ── Fond dynamique (élément créé par theme.js) ─────────────────── */
#ios26-backdrop {
  position: fixed;
  inset: 0;
  z-index: -1;
  overflow: hidden;
  background: linear-gradient(
    160deg,
    rgba(var(--spice-rgb-button), 0.35),
    var(--spice-main) 70%
  );
}
#ios26-backdrop .layer {
  position: absolute;
  inset: -8%;
  background-size: cover;
  background-position: center;
  filter: blur(72px) saturate(1.6);
  opacity: 0;
  transition: opacity 0.8s ease;
}
#ios26-backdrop .layer.visible {
  opacity: 1;
}
#ios26-backdrop::after {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(var(--spice-rgb-main), 0.52);
}

/* ── Panneaux flottants en verre ────────────────────────────────── */
.Root__top-container {
  padding: var(--ios-gap);
  gap: var(--ios-gap);
  --panel-gap: var(--ios-gap);
}

.Root__nav-bar,
.Root__main-view,
.Root__right-sidebar,
.Root__globalNav {
  background: var(--glass-bg) !important;
  -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.8);
  backdrop-filter: blur(var(--glass-blur)) saturate(1.8);
  border-radius: var(--ios-radius-lg);
  border: 1px solid var(--glass-border);
  box-shadow: var(--ios-shadow), inset 0 1px 0 var(--glass-highlight);
  overflow: hidden;
}

/* Grain de verre */
.Root__nav-bar::after,
.Root__main-view::after,
.Root__right-sidebar::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0.035;
  background: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='160' height='160'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='2'/><feColorMatrix type='saturate' values='0'/></filter><rect width='160' height='160' filter='url(%23n)'/></svg>");
}
.Root__nav-bar,
.Root__main-view,
.Root__right-sidebar {
  position: relative;
}

/* Les surfaces internes laissent voir le verre */
.Root__nav-bar nav,
.main-view-container,
.main-view-container__scroll-node,
.main-view-container__scroll-node-child > *,
.under-main-view,
.main-entityHeader-background,
.main-actionBarBackground-background,
.main-topBar-background {
  background: transparent !important;
  background-image: none !important;
}
```

- [ ] **Step 2: Appliquer et vérifier**

Run: `spicetify apply`
Expected: les trois panneaux (sidebar, vue principale, Now Playing) sont détachés des bords, arrondis, translucides avec un dégradé bleu nuit visible au travers ; bordure claire de 1 px en haut de chaque panneau.

- [ ] **Step 3: Commit**

```bash
git add user.css
git commit -m "feat: panneaux de verre flottants et fond translucide"
```

### Task 3: user.css — îlot de lecture + composants iOS

**Files:**
- Modify: `user.css` (ajout en fin de fichier)

**Interfaces:**
- Consumes: variables `--ios-*` et `--glass-*` de la tâche 2.

- [ ] **Step 1: Ajouter l'îlot et les composants à user.css**

```css
/* ── Îlot de lecture (dock iOS) ─────────────────────────────────── */
.Root__now-playing-bar {
  margin: 0 var(--ios-gap) var(--ios-gap);
  border-radius: 28px;
  background: var(--glass-bg-strong) !important;
  -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.8);
  backdrop-filter: blur(var(--glass-blur)) saturate(1.8);
  border: 1px solid var(--glass-border);
  box-shadow: var(--ios-shadow), inset 0 1px 0 var(--glass-highlight);
  overflow: hidden;
}
.main-nowPlayingBar-nowPlayingBar {
  background: transparent !important;
  border-top: none !important;
}
.main-nowPlayingBar-nowPlayingBar .cover-art {
  border-radius: var(--ios-radius-sm);
}

/* ── Boutons en pilules ─────────────────────────────────────────── */
button {
  border-radius: 999px !important;
}
.main-playButton-PlayButton > button,
.main-playButton-button {
  box-shadow: 0 4px 16px rgba(var(--spice-rgb-button), 0.45);
}

/* ── Champs de recherche / formulaires ──────────────────────────── */
input,
.x-filterBox-filterInput,
.main-globalNav-searchInput {
  border-radius: 999px !important;
  background: rgba(var(--spice-rgb-card), 0.55) !important;
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
  border: 1px solid var(--glass-border) !important;
}

/* ── Cartes ─────────────────────────────────────────────────────── */
.main-card-card {
  background: rgba(var(--spice-rgb-card), 0.45);
  border-radius: var(--ios-radius-md);
  border: 1px solid rgba(var(--spice-rgb-text), 0.06);
  transition: transform 0.25s ease, box-shadow 0.25s ease,
    background 0.25s ease;
}
.main-card-card:hover {
  background: rgba(var(--spice-rgb-card), 0.7);
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 12px 32px rgba(var(--spice-rgb-shadow), 0.35);
}
.main-cardImage-image,
.main-card-card img {
  border-radius: var(--ios-radius-sm);
}

/* ── Lignes de piste sélectionnées ──────────────────────────────── */
.main-trackList-trackListRow {
  border-radius: var(--ios-radius-sm);
}
.main-trackList-trackListRow:hover,
.main-trackList-selected {
  background: rgba(var(--spice-rgb-text), 0.07) !important;
}

/* ── Menus contextuels, popovers, modales en verre ──────────────── */
#context-menu,
.main-contextMenu-menu,
.main-popover,
.popup,
.GenericModal > div {
  background: rgba(var(--spice-rgb-card), 0.72) !important;
  -webkit-backdrop-filter: blur(32px) saturate(1.8);
  backdrop-filter: blur(32px) saturate(1.8);
  border-radius: var(--ios-radius-md) !important;
  border: 1px solid var(--glass-border);
  box-shadow: var(--ios-shadow);
}
.main-contextMenu-menuItemButton {
  border-radius: var(--ios-radius-sm) !important;
}

/* ── Scrollbars fines façon iOS ─────────────────────────────────── */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}
::-webkit-scrollbar-track {
  background: transparent;
}
::-webkit-scrollbar-thumb {
  background: rgba(var(--spice-rgb-text), 0.22);
  border-radius: 4px;
  border: 2px solid transparent;
  background-clip: content-box;
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(var(--spice-rgb-text), 0.4);
  background-clip: content-box;
}

/* ── Barre de progression / volume ──────────────────────────────── */
.x-progressBar-progressBarBg > div > div,
.x-progressBar-sliderArea > div > div {
  background-color: var(--spice-button) !important;
}
.progress-bar__slider,
.x-progressBar-sliderArea button {
  box-shadow: 0 2px 8px rgba(var(--spice-rgb-shadow), 0.4);
}
```

- [ ] **Step 2: Appliquer et vérifier**

Run: `spicetify apply`
Expected: barre de lecture détachée en îlot arrondi avec marge tout autour ; boutons ronds, recherche en pilule de verre, cartes qui « décollent » au survol, clic droit ouvre un menu contextuel en verre flou.

- [ ] **Step 3: Commit**

```bash
git add user.css
git commit -m "feat: ilot de lecture et composants iOS"
```

### Task 4: theme.js — fond dynamique pochette

**Files:**
- Create: `theme.js`

**Interfaces:**
- Consumes: styles `#ios26-backdrop`, `.layer`, `.layer.visible` définis en tâche 2.
- Produces: élément DOM `#ios26-backdrop` contenant deux `div.layer`, préfixé au `<body>`.

- [ ] **Step 1: Écrire theme.js**

```js
// IOS26.EDAWARD — fond dynamique : pochette du morceau en cours, floutée.
(function IOS26Backdrop() {
  if (!(window.Spicetify && Spicetify.Player && Spicetify.Player.data !== undefined)) {
    setTimeout(IOS26Backdrop, 300);
    return;
  }

  const backdrop = document.createElement("div");
  backdrop.id = "ios26-backdrop";
  const layers = [document.createElement("div"), document.createElement("div")];
  for (const layer of layers) {
    layer.className = "layer";
    backdrop.appendChild(layer);
  }
  document.body.prepend(backdrop);

  let active = 0;
  let currentUrl = "";

  function artworkUrl() {
    const meta =
      Spicetify.Player.data &&
      Spicetify.Player.data.item &&
      Spicetify.Player.data.item.metadata;
    if (!meta) return "";
    const raw =
      meta.image_xlarge_url || meta.image_large_url || meta.image_url || "";
    return raw.replace("spotify:image:", "https://i.scdn.co/image/");
  }

  function update() {
    const url = artworkUrl();
    if (!url || url === currentUrl) return;
    currentUrl = url;
    const next = 1 - active;
    const img = new Image();
    img.onload = () => {
      layers[next].style.backgroundImage = 'url("' + url + '")';
      layers[next].classList.add("visible");
      layers[active].classList.remove("visible");
      active = next;
    };
    img.src = url;
  }

  Spicetify.Player.addEventListener("songchange", update);
  update();
})();
```

- [ ] **Step 2: Appliquer et vérifier**

Run: `spicetify apply`
Expected: en lançant un morceau, le fond derrière les panneaux devient la pochette très floutée ; au morceau suivant, fondu de ~0,8 s vers la nouvelle pochette. Sans lecture, dégradé bleu nuit.

- [ ] **Step 3: Commit**

```bash
git add theme.js
git commit -m "feat: fond dynamique base sur la pochette"
```

### Task 5: Schéma clair + README

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: schéma `[light]` de la tâche 1.

- [ ] **Step 1: Vérifier le schéma clair**

Run: `spicetify config color_scheme light; spicetify apply`
Expected: interface verre givré claire, texte noir lisible partout, accent `#007AFF`. Vérifier sidebar, playbar, menu contextuel, cartes.

- [ ] **Step 2: Corriger les contrastes si besoin**

Si un texte est illisible en clair, ajuster dans `user.css` les alphas des
`--glass-*` (ex. passer `--glass-bg` à `0.62`) — pas de nouvelles couleurs en dur.

- [ ] **Step 3: Revenir au sombre**

Run: `spicetify config color_scheme dark; spicetify apply`
Expected: retour au verre fumé.

- [ ] **Step 4: Écrire README.md**

```markdown
# IOS26.EDAWARD

Thème Spicetify inspiré du design « Liquid Glass » d'iOS 26 : panneaux de
verre translucides et flottants, îlot de lecture, accent bleu iOS et fond
dynamique tiré de la pochette du morceau en cours.

## Installation

Le dossier doit se trouver dans `%APPDATA%\spicetify\Themes\IOS26.EDAWARD`.

```
spicetify config current_theme IOS26.EDAWARD
spicetify config color_scheme dark
spicetify apply
```

## Schémas

- `dark` — verre fumé (défaut)
- `light` — verre givré : `spicetify config color_scheme light && spicetify apply`

## Fichiers

- `color.ini` — palettes
- `user.css` — matériau verre, disposition flottante, composants
- `theme.js` — fond dynamique (pochette floutée)
```

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: README du theme"
```
